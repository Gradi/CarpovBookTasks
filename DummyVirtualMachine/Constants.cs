namespace DummyVirtualMachine
{
    public static class Constants
    {
        public const int OpCodeSize = sizeof(byte);

        public const int PointerSize = sizeof(int);

        public const int WordSize = sizeof(int);
    }
}
